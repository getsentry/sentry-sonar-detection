; Sentry Sonar — display node (Waveshare ESP32-S3 1.54" e-Paper)
; Placeholder — verify board id and add libs during the firmware phase (see ../../PLAN.md).
[env:display-node]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino
monitor_speed = 115200
lib_deps =
  ; zinggjm/GxEPD2       ; e-paper driver (add during firmware phase)
