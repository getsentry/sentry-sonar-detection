; Sentry Sonar — sensor node (Freenove ESP32-S3 + LD2410C radar)
; Placeholder — verify board id and add libs during the firmware phase (see ../../PLAN.md).
[env:sensor-node]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino
monitor_speed = 115200
; No radar lib needed — presence is read straight off the LD2410C OUT pin
; with a single digitalRead(). WiFi/HTTP libs come from the arduino framework.
lib_deps =
