export default function App() {
  return (
    <main>
      <h1>Sentry Sonar</h1>
      <p>Meeting-room availability overview — dashboard scaffold.</p>
      {/* TODO: poll GET /rooms every 5–10s and render a grid of room cards
          (FREE / IN USE / offline). See PLAN.md → Implementation phases. */}
    </main>
  )
}
